<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="mapper.RegisterMapper" >

  <resultMap id="LoginMap" type="model.count_user" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <result column="playerid" property="playerid" jdbcType="INTEGER" />

    <result column="playercode" property="playercode" jdbcType="INTEGER" />

    <result column="username" property="username" jdbcType="VARCHAR" />

    <result column="playername" property="playername" jdbcType="VARCHAR" />

    <result column="ifmanager" property="ifmanager" jdbcType="INTEGER" />

  </resultMap>
  
  <sql id="loginmatirial">

  	playerid,playercode,username,playername,ifmanager

  </sql>
  
  <insert id="newplayer" parameterType="map" >
	
	insert into 
		
		count_user(playercode,username,userpassword,playername,ifmanager)
		
	values (#{playercode,jdbcType=INTEGER},#{username,jdbcType=VARCHAR},#{userpassword,jdbcType=VARCHAR},#{playername,jdbcType=VARCHAR},#{ifmanager,jdbcType=INTEGER})
	
  </insert>
  
  <select id="playerlogin" parameterType="map" resultMap="LoginMap">
		
		SELECT
		
		<include refid="loginmatirial"/>
		
		FROM
		
			count_user
			
		WHERE username LIKE #{username,jdbcType=VARCHAR} and userpassword LIKE #{userpassword,jdbcType=VARCHAR}
 
  </select>
  
  <select id="ifexist" parameterType="map" resultType="java.lang.Integer">
  
  		select count(*) from count_user where username like #{username,jdbcType=VARCHAR}
  
  </select>
  
   <select id="ifexistcode" parameterType="map" resultType="java.lang.Integer">
  
  		select count(*) from count_user where playercode = #{randomNum,jdbcType=INTEGER}
  
  </select>
  
 </mapper>